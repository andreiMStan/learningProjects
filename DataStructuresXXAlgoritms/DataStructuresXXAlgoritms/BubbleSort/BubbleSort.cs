

//using DataStructuresXXAlgoritms.BubbleSort;

//int[] intArray = new int[] { 5, 6, 7, 4, 3, 2, 1 };
////Console.WriteLine(BubbleSort(intArray));
//int[] value = BubbleSort(intArray);
//int joker = 1;
//int[] BubbleSort(int[] array)
//{
// // hold temporary swap variable . Think state
// int temp = 0;
// //iterates over entire loop many times
// for (int pointer = 0; pointer < array.Length; pointer++)
// {
//  for (int sort = 0; sort < array.Length - 1; sort++)
//  {
//   //this check to see if larger than the right
//   if (array[sort] > array[sort + 1])
//   {
//    //store variable as temp so we don't override it when we swap
//    temp = array[sort + 1];
//    array[sort + 1] = array[sort];
//    array[sort] = temp;
//   }
//  }
// }
// return array;
//}